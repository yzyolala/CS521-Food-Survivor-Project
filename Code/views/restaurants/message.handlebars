<main>
    <div class="card container" id="cont">
        <h1>Your Action was successful</h1>
        <h3>Restaurant will be listed on Restaurant List page</h3>
        <div>
            <p>{{message}}</p>
        </div>
        {{#if message}}
        <a class="logoutShiftLine" href="/restaurants">Go to the restaurants</a>
        {{/if}}
    </div>
    <a class="btn btn-primary" href="/logout">Logout</a>
</main>