{{!-- <main>
    {{#each restaurantsList}}
    <div class="card-deck">
        <div class="col-sm-6">
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title"><a href="/restaurants/{{this._id}}">{{this.name}}</a></h5>
                    <p class="card-text">{{this.address}}</p>
                    <p class="card-text">Menu: {{this.foodmenu}}</p>
                    <p class="card-text">Rating: {{this.rating}}</p>
                </div>
            </div>
        </div>
        <script src="../../public/javascript/filter.js"></script>
    </div>
    {{/each}}
    <br>
    <br>
    <br>
    <br>

</main> --}}

<div class="container-xxl py-5">
    <div class="container">
        <div class="row g-0 gx-5">
            <div class="col-lg-8">
                <div class="section-header text-start mb-5 wow fadeInUp" data-wow-delay="0.1s"
                    style="max-width: 600px;">
                    <h1 class="display-5 mb-6 text-secondary">Restaurants' <span class="bg-green">List</span></h1>
                    <p>View restaurants that have mind blowing food that is made with love and hygine. You can search
                        restaurants based on name or the menu you wish and be a food survivor.</p>
                </div>
            </div>
            <div class="col-lg-4 py-4">
                <a href="/buying/pickRestaurant" class="btn btn-primary rounded-pill">Order Magic Box</a><br>
                <p class="form-text text-muted">Click to order your magic box from any restaurant listed</p>
            </div>
        </div>
        <div class="row g-4">
            {{#each restaurantsList}}
            <div class="col-xl-3 col-lg-4 col-md-6 wow fadeInUp" data-wow-delay="0.1s">
                <div class="product-item">
                    <div class="position-relative bg-light overflow-hidden">
                        <img class="img-fluid w-100" src="{{!-- Add image here --}}" alt="">
                        <div class="bg-secondary rounded text-white position-absolute start-0 top-0 m-4 py-1 px-3">New
                        </div>
                    </div>
                    <div class="p-4">
                        <a class="d-block h5 mb-2 text-center" href="/restaurants/{{this._id}}">{{this.name}}</a>
                        <p><span class="text-primary me-1">Address:</span> {{this.address}}</p>
                        <p><span class="text-secondary me-1">Menu list:</span> {{this.description}}</p>
                        <p><span class="text-secondary me-1"></span></p>
                        <table>
                            <th>menu</th>
                            <th>count</th>{{#each this.restaurantdishlist}}
                            {{!-- <p><span class="text-secondary me-1"></span> --}}

                                <tr>
                                    <td>{{this.dishname}} </td>
                                    <td> {{this.count}}</td>
                                </tr>
                            </p>
                            {{!-- <p><span class="text-secondary me-1">count:</span> {{this.count}}</p> --}}
                            {{!-- <br> --}}
                            {{/each}}
                        </table>
                        <br>
                        <p><span class="bg-green me-1">Rating:</span> {{this.rating}}</p>
                        {{!-- <span class="text-body text-decoration-line-through">Address</span> --}}
                    </div>
                    <div class="d-flex border-top">
                        <div class="w-50 text-center py-2">
                            <a class="text-body" href="/restaurants/{{this._id}}"><i
                                    class="fa fa-eye text-primary me-2"></i>Visit Restaurant</a>
                        </div>
                        {{!-- <small class="w-50 text-center py-2">
                            <a class="text-body" href=""><i class="fa fa-shopping-bag text-primary me-2"></i>Rate</a>
                        </small> --}}
                    </div>
                </div>
                <script src="../../public/javascript/filter.js"></script>
            </div>
            {{/each}}
        </div>
    </div>
</div>
<a href="#" class="btn btn-lg btn-primary btn-lg-square rounded-circle back-to-top"><i class="bi bi-arrow-up"></i></a>